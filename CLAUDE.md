# PR Workflow

- Always `git pull --rebase` before push, NEVER `--force`
- Netlify: Auto-Deploy is DISABLED — always publish manually after push
- Never expose API keys with `VITE_` prefix in frontend bundle
- Stack: React/Vite/TypeScript, Netlify Functions, ElevenLabs, Anthropic Claude
- Full project context: see PR-CONTEXT.md
